---

wildfly_package_name: "wildfly-{{ wildfly_version }}"
wildfly_package: "{{ wildfly_package_name }}.tar.gz"
wildfly_url: "http://download.jboss.org/wildfly/{{ wildfly_version }}/{{ wildfly_package }}"

wildfly_default_templates:
  - wildfly.conf
  - standalone.conf
